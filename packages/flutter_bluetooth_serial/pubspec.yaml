name: flutter_bluetooth_serial
version: 0.4.0
description: "Local stub of flutter_bluetooth_serial for development (simulated Bluetooth)"
environment:
  sdk: "^3.9.2"

# No flutter assets or native code; this is a Dart-only stub to allow app to run.

dependencies:
  flutter:
    sdk: flutter
